import React, { Component } from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';

class FlexiIcon extends Component {
  render() {
    return (
      <Icon {...this.props} />
    );
  }
}

export default FlexiIcon;
